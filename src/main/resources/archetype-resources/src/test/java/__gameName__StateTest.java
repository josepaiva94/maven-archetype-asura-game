#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
package ${package};

import org.junit.Assert;
import org.junit.Test;

/**
 * Test the state of the ${gameName}.
 *
 * @author ${author} <code>${authorEmail}</code>
 */
public class ${gameName}StateTest {

    @Test
    public void rigorousTest() {
        Assert.assertTrue(true);
    }
}
